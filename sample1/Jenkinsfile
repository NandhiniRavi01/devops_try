pipeline {
    agent any

    environment {
        IMAGE_TAG = "latest"
        
        ARGOCD_SERVER = "localhost:8081"
    }

    stages {
        stage('Checkout') {
            steps {
                git credentialsId: 'git-credential', 
                    url: 'https://github.com/NandhiniRavi01/devops_try',
                    branch: 'main'
            }
        }

        stage('Build Docker') {
            steps {
                script {
                    sh '''
                    echo "Building Docker Image"
                    docker build -t devopstry:${IMAGE_TAG} -f sample1/Dockerfile .
                    '''
                }
            }
        }

        stage('Run Docker Container') {
            steps {
                script {
                    echo 'Running Docker Container'
                    sh 'docker run -d -p 5000:5000 --name myapp devopstry:latest
'
                }
            }
        }
    

       stage('Build') {
            steps {
                git branch: 'main', url: 'https://github.com/NandhiniRavi01/devops_try.git'
                sh 'python3 app.py'
            }
        }
        stage('Test') {
            steps {
                sh 'python3 -m pytest'
            }
        }
    }
}

pipeline {
    agent any
    environment {
        SUDO_PASSWORD = credentials('sudo-credentials')  // Replace 'sudo-password-id' with your Jenkins credential ID
    }
    
   
        
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/NandhiniRavi01/devops_try'
            }
        }
       stage('Install Node.js') {
            steps {
                script {
                    // Use the 'expect' script if you need to handle the sudo password
                    sh "./run_command_with_sudo.exp ${SUDO_PASSWORD} 'curl -fsSL https://deb.nodesource.com/setup_20.x | bash -'"
                    sh "./run_command_with_sudo.exp ${SUDO_PASSWORD} 'apt-get install -y nodejs'"
                }
            }
        }
        


        // Stage to verify the installation of Node.js
        stage('Verify Node.js Installation') {
            steps {
                // Verify Node.js and npm installation
                sh 'node -v'
                sh 'npm -v'
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    // Build the Docker image
                    sh 'docker build -t nodejs-app .'
                }
            }
        }

        stage('Run Docker Container') {
            steps {
                script {
                    // Run the Docker container
                    sh 'docker run -d -p 3000:3000 nodejs-app'
                }
            }
        }
    }

    post {
        always {
            echo 'Pipeline complete.'
        }
    }
}
